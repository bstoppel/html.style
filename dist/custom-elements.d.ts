// Generated by build.js from custom-elements.json. Do not edit.
//
// Declaring the elements once is what a framework wrapper would mostly have
// bought: HTMLElementTagNameMap types every document.querySelector and
// createElement call, in plain TypeScript and inside any framework.

export interface HsAccordionElementEventMap {
  /** Fired when a panel opens or closes. `detail` carries `{ index, open }`. */
  'hs-accordion-toggle': Event;
}

export interface HsAccordionElement extends HTMLElement {
  /** The `<details>` children, in document order. */
  panels: HTMLDetailsElement[];
  openAll(): void;
  closeAll(): void;

  addEventListener<K extends keyof HsAccordionElementEventMap>(
    type: K,
    listener: (this: HsAccordionElement, event: HsAccordionElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsAccordionElementEventMap>(
    type: K,
    listener: (this: HsAccordionElement, event: HsAccordionElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsAlertElementEventMap {
  /** Cancelable, fired before the alert removes itself. Call preventDefault() to keep it in place. */
  'hs-dismiss': CustomEvent;
}

export interface HsAlertElement extends HTMLElement {
  /** Remove the alert. Fires a cancelable `hs-dismiss` first so a consumer can animate the exit, or call preventDefault() to keep it in place. */
  dismiss(): void;

  addEventListener<K extends keyof HsAlertElementEventMap>(
    type: K,
    listener: (this: HsAlertElement, event: HsAlertElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsAlertElementEventMap>(
    type: K,
    listener: (this: HsAlertElement, event: HsAlertElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

/** <hs-badge> — CSS-only element. No behaviour, no shadow root. The global stylesheet styles `hs-badge` directly, so it renders with JavaScript disabled and without this module. The class exists only so the element reaches custom-elements.json for editor completion; rendering never depends on registration. See hs-card.js for the reasoning behind this tier. */
export interface HsBadgeElement extends HTMLElement {
}

/** <hs-card> — CSS-only element. There is no behaviour here and no shadow root. The global stylesheet styles `hs-card` directly, so the element renders correctly with JavaScript disabled, before any script loads, and if this module is never imported at all. The class below exists ONLY so the element appears in custom-elements.json and editors offer completion for it. Nothing about rendering depends on it being registered — delete the import and the card still looks exactly the same. Why an element rather than `class="card"`: the tag name carries the meaning, which is what a machine reads first, and it keeps one authoring vocabulary — everything is `<hs-*>` whether or not it has behaviour. The `.card` class stays supported. */
export interface HsCardElement extends HTMLElement {
}

export interface HsComboboxElementEventMap {
  /** Fired after the listbox opens. */
  'hs-open': Event;
  /** Fired after it closes. */
  'hs-close': Event;
  /** Fired when the chosen value changes, like a native control. */
  change: Event;
}

export interface HsComboboxElement extends HTMLElement {
  formAssociated: boolean;
  value: string;
  label: string;
  placeholder: string;
  disabled: boolean;
  open: boolean;
  name: string;
  formResetCallback(): void;
  formStateRestoreCallback(state: unknown): void;
  /** Re-read the `<hs-option>` children. Call after changing them. */
  refresh(): void;

  addEventListener<K extends keyof HsComboboxElementEventMap>(
    type: K,
    listener: (this: HsComboboxElement, event: HsComboboxElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsComboboxElementEventMap>(
    type: K,
    listener: (this: HsComboboxElement, event: HsComboboxElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsCopyElementEventMap {
  /** Fired when the copy fails, which the Clipboard API does whenever the document is not focused or permission is refused. */
  'hs-copy-error': CustomEvent;
  /** Fired after a successful copy. `detail` carries `{ text }`. */
  'hs-copy': CustomEvent;
}

export interface HsCopyElement extends HTMLElement {
  /** The text this element would copy. */
  text: string;
  copy(): void;

  addEventListener<K extends keyof HsCopyElementEventMap>(
    type: K,
    listener: (this: HsCopyElement, event: HsCopyElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsCopyElementEventMap>(
    type: K,
    listener: (this: HsCopyElement, event: HsCopyElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsDialogElementEventMap {
  /** Fired after the dialog opens. */
  'hs-open': CustomEvent;
  /** Fired after it closes. `detail` carries `{ returnValue }`. */
  'hs-close': Event;
}

/** <hs-dialog> — light DOM, wrapping a real <dialog>. The platform already does the hard parts: the top layer, the backdrop, the focus trap, inert-ing the page behind, Escape to close, returning focus to whatever opened it, and `<form method="dialog">`. None of that is reimplemented here — a native <dialog> does it, and this element wraps one. What it adds is the part the platform leaves to script: - `<dialog open>` shows NON-modally. Getting a modal requires calling showModal() from JavaScript, so there is no declarative way to open one. The `open` attribute here means modal. - Backdrop dismissal. Where the browser supports `closedBy` this sets it and the PLATFORM does the dismissing; the JavaScript fallback below only runs on browsers at this project's floor, which predate it. Children are moved into the <dialog> once, on connect, so consumers write their content directly and `<form method="dialog">` still finds its ancestor: <hs-dialog id="confirm"> <h2>Delete this?</h2> <form method="dialog"> <button value="cancel">Cancel</button> <button value="delete">Delete</button> </form> </hs-dialog> */
export interface HsDialogElement extends HTMLElement {
  /** The wrapped native element, for anything this API does not cover. */
  dialog: HTMLDialogElement | null;
  open: boolean;
  /** Open as a modal. Idempotent. */
  show(): void;
  /** Close, optionally setting the native returnValue. */
  close(returnValue: unknown): void;

  addEventListener<K extends keyof HsDialogElementEventMap>(
    type: K,
    listener: (this: HsDialogElement, event: HsDialogElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsDialogElementEventMap>(
    type: K,
    listener: (this: HsDialogElement, event: HsDialogElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsFieldElementEventMap {
  /** Fired when the control fails validation. `detail` carries `{ message }`, the browser's own text. */
  'hs-invalid': Event;
}

export interface HsFieldElement extends HTMLElement {
  /** The wrapped native control. */
  control: HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement | null;

  addEventListener<K extends keyof HsFieldElementEventMap>(
    type: K,
    listener: (this: HsFieldElement, event: HsFieldElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsFieldElementEventMap>(
    type: K,
    listener: (this: HsFieldElement, event: HsFieldElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsMenuElementEventMap {
  /** Fired after the menu opens. */
  'hs-open': Event;
  /** Fired after it closes. */
  'hs-close': Event;
  /** Fired when an item is chosen. `detail` carries `{ value, label, index }`. */
  'hs-menu-select': CustomEvent;
}

export interface HsMenuElement extends HTMLElement {
  label: string;
  disabled: boolean;
  open: boolean;
  /** Re-read the `<hs-menu-item>` children. Call after changing them. */
  refresh(): void;

  addEventListener<K extends keyof HsMenuElementEventMap>(
    type: K,
    listener: (this: HsMenuElement, event: HsMenuElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsMenuElementEventMap>(
    type: K,
    listener: (this: HsMenuElement, event: HsMenuElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

/** One command in an `<hs-menu>`. Deliberately inert: it carries a value and its text, and the menu does the rest. It never renders — the global stylesheet hides it, and once the menu upgrades it is not slotted at all. */
export interface HsMenuItemElement extends HTMLElement {
}

/** One choice in an `<hs-combobox>`. Deliberately inert: it carries a value and its text, and the combobox does the rest. It never renders — the global stylesheet hides it, and once the combobox upgrades it is not slotted at all. */
export interface HsOptionElement extends HTMLElement {
}

export interface HsSortableElementEventMap {
  /** Fired after a sort. `detail` carries `{ column, direction, header }`. */
  'hs-sort': CustomEvent;
}

export interface HsSortableElement extends HTMLElement {
  /** The wrapped table, for anything this API does not cover. */
  table: HTMLTableElement | null;
  /** The header of the sorted column, or null while nothing is sorted. */
  sortedBy: HTMLTableCellElement | null;
  /** Re-read the headers. Call after replacing the table or its `<thead>`. */
  refresh(): void;
  /** Sort by a column. */
  sort(header: HTMLTableCellElement|number, direction?: 'ascending'|'descending'): void;

  addEventListener<K extends keyof HsSortableElementEventMap>(
    type: K,
    listener: (this: HsSortableElement, event: HsSortableElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsSortableElementEventMap>(
    type: K,
    listener: (this: HsSortableElement, event: HsSortableElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

/** A panel's content holder. Deliberately inert: it carries a label and its children, and `hs-tabs` does the rest. */
export interface HsTabPanelElement extends HTMLElement {
}

export interface HsTabsElementEventMap {
  /** Fired after the active tab changes. `detail` carries `{ index, label }`. */
  'hs-tab-change': CustomEvent;
}

export interface HsTabsElement extends HTMLElement {
  /** The `<hs-tab-panel>` children, in document order. */
  panels: HTMLElement[];
  selected: number;
  activation: string;

  addEventListener<K extends keyof HsTabsElementEventMap>(
    type: K,
    listener: (this: HsTabsElement, event: HsTabsElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsTabsElementEventMap>(
    type: K,
    listener: (this: HsTabsElement, event: HsTabsElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsThemeToggleElementEventMap {
  /** Fired after the scheme changes. `detail` carries `{ scheme }`, one of `light` or `dark`. */
  'hs-theme-change': Event;
}

export interface HsThemeToggleElement extends HTMLElement {
  /** The scheme in effect: an explicit choice, else the system preference. */
  scheme: 'light' | 'dark';
  toggle(): void;

  addEventListener<K extends keyof HsThemeToggleElementEventMap>(
    type: K,
    listener: (this: HsThemeToggleElement, event: HsThemeToggleElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsThemeToggleElementEventMap>(
    type: K,
    listener: (this: HsThemeToggleElement, event: HsThemeToggleElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsToastElementEventMap {
  /** Cancelable, fired before the toast removes itself. Call preventDefault() to keep it in place. */
  'hs-dismiss': CustomEvent;
}

export interface HsToastElement extends HTMLElement {
  /** Milliseconds before self-dismissal. Zero means it stays. */
  duration: number;
  /** Milliseconds left on the clock. Stops falling while paused. */
  remaining: number;
  /** Whether the timeout is currently held by a pointer or by focus. */
  paused: boolean;
  /** Remove it. Fires a cancelable `hs-dismiss` first, matching `<hs-alert>`, so a consumer can keep it in place. */
  dismiss(): void;

  addEventListener<K extends keyof HsToastElementEventMap>(
    type: K,
    listener: (this: HsToastElement, event: HsToastElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsToastElementEventMap>(
    type: K,
    listener: (this: HsToastElement, event: HsToastElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsToastRegionElement extends HTMLElement {
  /** The toasts currently showing, oldest first. */
  toasts: HTMLElement[];
  /** Show a toast and return it, so the caller can dismiss it early or listen to it. Appending keeps DOM order the same as the order they were shown. The options are one inline type rather than `@param options.variant` entries. The analyzer mis-parses those against a destructured parameter: it emitted a parameter literally named `{ variant, duration }` and wrote raw JSDoc into the manifest as a type, which then reached the generated declarations and stopped them parsing. */
  show(message: string, options?: { variant?: 'success'|'warning'|'error'|'info', duration?: number }): HsToastElement;
  /** Dismiss everything showing. Respects a cancelled `hs-dismiss`. */
  clear(): void;
}

export interface HsToggleElementEventMap {
  /** Fired when the state changes, like a native control. */
  change: Event;
}

export interface HsToggleElement extends HTMLElement {
  formAssociated: boolean;
  checked: boolean;
  disabled: boolean;
  value: string;
  role: string;
  name: string;
  /** Flip the switch, unless disabled. Fires `change` like a native control. */
  toggle(): void;
  formResetCallback(): void;
  formStateRestoreCallback(state: unknown): void;

  addEventListener<K extends keyof HsToggleElementEventMap>(
    type: K,
    listener: (this: HsToggleElement, event: HsToggleElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsToggleElementEventMap>(
    type: K,
    listener: (this: HsToggleElement, event: HsToggleElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

export interface HsTooltipElementEventMap {
  /** Fired after the tooltip appears. */
  'hs-open': Event;
  /** Fired after it hides. */
  'hs-close': Event;
}

export interface HsTooltipElement extends HTMLElement {
  /** The element this describes, or null while `for` names nothing. */
  trigger: HTMLElement | null;
  /** Show it. An arrow field, so it doubles as the listener. Safe to call when it is already showing. */
  show: () => void;
  /** Hide it. Safe to call when it is already hidden. */
  hide: () => void;

  addEventListener<K extends keyof HsTooltipElementEventMap>(
    type: K,
    listener: (this: HsTooltipElement, event: HsTooltipElementEventMap[K]) => unknown,
    options?: boolean | AddEventListenerOptions
  ): void;
  addEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | AddEventListenerOptions
  ): void;
  removeEventListener<K extends keyof HsTooltipElementEventMap>(
    type: K,
    listener: (this: HsTooltipElement, event: HsTooltipElementEventMap[K]) => unknown,
    options?: boolean | EventListenerOptions
  ): void;
  removeEventListener(
    type: string,
    listener: EventListenerOrEventListenerObject,
    options?: boolean | EventListenerOptions
  ): void;
}

declare global {
  interface HTMLElementTagNameMap {
    'hs-accordion': HsAccordionElement;
    'hs-alert': HsAlertElement;
    'hs-badge': HsBadgeElement;
    'hs-card': HsCardElement;
    'hs-combobox': HsComboboxElement;
    'hs-copy': HsCopyElement;
    'hs-dialog': HsDialogElement;
    'hs-field': HsFieldElement;
    'hs-menu': HsMenuElement;
    'hs-menu-item': HsMenuItemElement;
    'hs-option': HsOptionElement;
    'hs-sortable': HsSortableElement;
    'hs-tab-panel': HsTabPanelElement;
    'hs-tabs': HsTabsElement;
    'hs-theme-toggle': HsThemeToggleElement;
    'hs-toast': HsToastElement;
    'hs-toast-region': HsToastRegionElement;
    'hs-toggle': HsToggleElement;
    'hs-tooltip': HsTooltipElement;
  }
}

// Augments react's OWN "JSX" namespace rather than the bare global one -
// react-jsx (the modern transform, and the default in most current
// setups) resolves IntrinsicElements through here, re-exported from
// "react/jsx-runtime", not through "declare global". Inert wherever
// "react" is not installed at all: an augmentation of an unresolvable
// module specifier is not an error unless something imports it.
declare module 'react' {
  namespace JSX {
    interface IntrinsicElements {
    'hs-accordion': {
      /** Only one panel open at a time, via the native `name` grouping. */
      exclusive?: boolean;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-alert': {
      /** Render a dismiss button. */
      dismissible?: boolean;
      /** Accessible name for the dismiss button. Defaults to "Dismiss". */
      'dismiss-label'?: string;
      /** Feedback tone. Styled by the global stylesheet, so it applies before this module loads. */
      variant?: 'success'|'warning'|'error'|'info';
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-badge': {
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-card': {
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-combobox': {
      /** Field name used when the form is submitted. */
      name?: string;
      /** Value of the chosen option. Reflected. */
      value?: string;
      /** Visible label. Rendered as a real `<label for>` inside the shadow root, where the association works. */
      label?: string;
      /** Placeholder text for the input. */
      placeholder?: string;
      /** Inactive and removed from the tab order. */
      disabled?: boolean;
      /** Whether the listbox is showing. Reflected. */
      open?: boolean;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-copy': {
      /** Id of the element whose text is copied. Without it, the copied text is this element's own `value` attribute. */
      for?: string;
      /** Literal text to copy, used when `for` is absent. */
      value?: string;
      /** Confirmation text. Defaults to "Copied". */
      'copied-label'?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-dialog': {
      /** Present means the dialog is open, as a modal. */
      open?: boolean;
      /** Do not close on backdrop click. Escape still works; the platform owns that and it is not overridden. */
      persistent?: boolean;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-field': {
      /** Label text. Skipped if you supply your own <label>. */
      label?: string;
      /** Help text shown under the control and referenced by aria-describedby. */
      hint?: string;
      /** Do not display validation messages. The control still validates; only this element's reporting is suppressed. */
      novalidate?: boolean;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-menu': {
      /** Text on the trigger button. Also what the pre-upgrade rule in the global stylesheet draws, so the box is reserved at the right width. */
      label?: string;
      /** Inactive and removed from the tab order. */
      disabled?: boolean;
      /** Whether the menu is showing. Reflected. Setting it opens or closes the menu. */
      open?: boolean;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-menu-item': {
      /** Value reported when this item is chosen. Defaults to the element's text content. */
      value?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-option': {
      /** Value submitted when this option is chosen. Defaults to the element's text content. */
      value?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-sortable': {
      /** On a `<th>`, not on this element: marks the column sortable. `data-sort="number"` compares as numbers instead of collating. */
      'data-sort'?: string;
      /** On a `<td>`, not on this element: an explicit sort key for that cell, used instead of its text. */
      'data-sort-value'?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-tab-panel': {
      /** Text shown on this panel's tab. */
      label?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-tabs': {
      /** Index of the active tab. Reflected. */
      selected?: number;
      /** `auto` (default) selects on arrow-key focus; `manual` requires Enter or Space. */
      activation?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-theme-toggle': {
      /** Accessible name for the button. Defaults to "Toggle colour scheme". */
      label?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-toast': {
      /** Feedback tone, styled by the global stylesheet. `error` also makes this toast announce assertively. */
      variant?: 'success'|'warning'|'error'|'info';
      /** Milliseconds before it dismisses itself. Defaults to 5000. Zero means it stays until something dismisses it. */
      duration?: number;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-toast-region': {
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-toggle': {
      /** Whether the switch is on. */
      checked?: boolean;
      /** Inactive and removed from the tab order. */
      disabled?: boolean;
      /** Field name used when the form is submitted. */
      name?: string;
      /** Value submitted when checked. Defaults to "on". */
      value?: string;
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    'hs-tooltip': {
      /** Id of the element this describes. Required: without a trigger there is nothing to attach to and the tooltip stays hidden. */
      for?: string;
      /** Preferred side. Defaults to `block-end`, and flips when there is no room. */
      placement?: 'block-end'|'block-start';
      children?: unknown;
      ref?: unknown;
      key?: unknown;
      [prop: string]: unknown;
    };
    }
  }
}
